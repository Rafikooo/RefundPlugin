{% set order = hookable_metadata.context.order %}

{% for item in order.items %}
    {% for unit in item.units %}
        {% set variant = item.variant %}
        {% set product = variant.product %}
        <tr class="unit">
            {% hook 'products' with { product, variant, unit } %}
        </tr>
    {% endfor %}
{% endfor %}
